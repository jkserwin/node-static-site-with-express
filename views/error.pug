extends layout.pug 

block content 

    nav.nav
        a(href='/')
            span &larr;
            |  Back

    article.portfolio-projects
        .inner-wrapper.flex-row-wrap.two-col
        .project-info-box.box
            h1 Oops! Something went wrong.
            h2= `${err.message} - Status: ${err.status}`
            p= err.stack

        nav.nav
        a(href='/')
            span &larr;
            |  Back